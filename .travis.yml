#
# Travis configuration
#

# Source related
language: cpp
# Defined in CXX
compiler:
    - gcc
    - clang

install: "cd cpp && make && cd .."
script: "cd cpp && make check && cd .."

# Branches
branches:
    only:
        - master
        - next
        - util/testing
